{
  "name": "scan-ytb",
  "version": "5.2.0",
  "private": true,
  "description": "ScanYTB - paid YouTube intelligence searches with accounts, credits, admin access and Dodo Payments.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/youtube.js && node --check src/repository.js && node --check src/links.js && node --check src/account.js && node --check public/app.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3",
    "bcryptjs": "^2.4.3"
  }
}
