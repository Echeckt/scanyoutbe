{
  "name": "youtube-ecom-fr-scanner",
  "version": "2.0.0",
  "private": true,
  "description": "Discover French-speaking ecommerce YouTube channels and extract links from video descriptions.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/youtube.js && node --check src/repository.js && node --check src/links.js && node --check public/app.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
